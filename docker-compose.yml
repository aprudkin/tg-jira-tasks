services:
  bot:
    container_name: tg-jira-bot
    build: .
    env_file:
      - .env
    restart: always
    stop_grace_period: 10s
    mem_limit: 256m
    cpus: "0.5"
    volumes:
      - bot_data:/app/data

volumes:
  bot_data:
